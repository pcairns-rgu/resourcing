CREATE TABLE private_list(
                           id int(7) NOT NULL AUTO_INCREMENT,
                           today DATE,
                           task varchar(250),
                           comments varchar(250),
                           deadline date,
                           PRIMARY KEY(id)
)
